/**
 * App-wide type aliases.
 */

export type ToastKind = "info" | "error" | "success" | "warning";

export type SectionId = "hotkeys" | "window" | "timing";

/** Window-keyword input mode in WindowPage. */
export type DraftMode = "fuzzy" | "exact";
