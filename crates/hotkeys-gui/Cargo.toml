[package]
name = "hotkeys-gui"
version = "0.1.0"
edition = "2024"

[[bin]]
name = "hotkeys-gui"
path = "src/main.rs"

[dependencies]
hotkeys-core = { path = "../hotkeys-core" }
eframe = { version = "0.32", default-features = false, features = [
    "default_fonts",
    "wgpu",
] }
egui = "0.32"
