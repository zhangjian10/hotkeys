{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "默认窗口的能力配置",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:window:default"
  ]
}
